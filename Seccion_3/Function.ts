function sumar(a:number, b:number):number {
    return a+b;
}

const restar = (x:number, y:number):number => {
    return x+y;
}

const suma:number = sumar(2,2);
const resta:number = restar(7,3);

console.log(suma);
console.log(resta);